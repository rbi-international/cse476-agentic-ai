# CSE476 Agentic AI and Intelligent Automation
# Install:  pip install -r requirements.txt
# Verified working 8 August 2026 (Python 3.12).

# --- core, needed from Unit 1 ---
openai>=2.0.0
python-dotenv>=1.0.1
pydantic>=2.9
httpx>=0.27
rich>=13.7
tiktoken>=0.8

# --- notebooks, from Unit 1 ---
jupyter>=1.1
ipykernel>=6.29

# --- the real frameworks, from Unit 3 onward ---
# Semantic Kernel and AutoGen merged into Microsoft Agent Framework 1.0 in
# April 2026. We build on Agent Framework, and keep semantic-kernel because it
# is the foundation layer underneath it (Unit 3 Lecture 2).
#
# We install the CORE and the OpenAI connector, not the full "agent-framework"
# meta-package. The meta-package pulls in every integration Microsoft ships
# (azure-functions, bedrock, cosmos, and dozens more), and its azure-functions
# piece pins a werkzeug version that conflicts with semantic-kernel's, which
# makes the whole install fail. We only ever import agent_framework.openai, so
# these two packages are all we need, and they resolve cleanly next to SK.
agent-framework-core>=1.12
agent-framework-openai>=1.11
semantic-kernel>=1.44

# --- testing and linting, used throughout ---
pytest>=8.3
ruff>=0.8

# --- Unit 5, serving and deployment ---
# Unit 5 wraps the agent in a real API (Lecture 4) and packages it (Lecture 5).
# FastAPI serves the agent behind a typed contract; uvicorn runs it. Both are
# tested offline with FastAPI's TestClient, so no server is needed for the tests.
fastapi>=0.115
uvicorn>=0.32

# --- Unit 5, observability (optional, referenced not required) ---
# The observability lecture (L3) builds its own lightweight trace/metrics/logs in
# pure Python so it runs anywhere. These are the production-grade equivalents you
# would reach for in a real deployment; uncomment if you want to use them.
# opentelemetry-sdk>=1.29
# prometheus-client>=0.21
